---
import Nav from "../components/navbar/Nav";
import TopButton from "../components/TopButton";
import Pointer from "../components/Pointer";
import ChatWidget from "../components/chatbot/ChatWidget";
import FooterBar from "../components/footer/FooterBar";

const { title, footerVariant = "default" } = Astro.props;
const posts = await Astro.glob("../pages/posts/*.md");
const pages = [
  {
    title: "Home",
    description: "Landing, hero, and quick intro.",
    url: "/",
  },
  {
    title: "About",
    description: "Bio, education, skills, and experience.",
    url: "/#about",
  },
  {
    title: "Projects",
    description: "AI, research, and industry projects.",
    url: "/projects",
  },
  {
    title: "Filmmaking",
    description: "Among Monsters and film work.",
    url: "/filmmaking",
  },
  {
    title: "Writing",
    description: "Blog posts and research notes.",
    url: "/posts",
  },
  {
    title: "Contact",
    description: "Email and social links.",
    url: "/#contact",
  },
];

const searchItems = [
  ...pages.map((page) => ({ ...page, type: "page" })),
  ...posts.map((post) => ({
    title: post.frontmatter.title,
    description: post.frontmatter.description,
    url: post.url,
    type: "post",
  })),
];

import "../styles/global.css";
---

<!doctype html>
<html lang="en" translate="no">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="description"
      content="Lead AI Engineer specializing in agentic AI, RAG, and ML systems."
    />
    <meta name="keywords" content="Sampreeth Avvari, AI engineer, agentic AI, RAG, ML, MLOps, NYU" />
    <meta name="author" content="Sampreeth Avvari" />
    <meta http-equiv="Content-Language" content="en" />
    <meta name="google" content="notranslate" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link
      rel="alternate"
      type="application/rss+xml"
      title="Sampreeth Avvari RSS"
      href={`${Astro.site}rss.xml`}
    />
    <meta name="generator" content={Astro.generator} />
    <script type="text/javascript">
      (function () {
        var css = document.createElement("link");
        css.href =
          "https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css";
        css.rel = "stylesheet";
        css.integrity =
          "sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==";
        css.crossOrigin = "anonymous";
        css.referrerPolicy = "no-referrer";
        document.head.appendChild(css);
      })();
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/normalize.css@8.0.1/normalize.min.css"
      rel="stylesheet"
    />
    <title>{title}</title>
    <script>
      // On page load or when changing themes, best to add inline in `head` to avoid FOUC
      if (
        localStorage.getItem("theme") === "dark" ||
        (!("theme" in localStorage) &&
          window.matchMedia("(prefers-color-scheme: dark)").matches)
      ) {
        document.documentElement.classList.add("dark");
      } else {
        document.documentElement.classList.remove("dark");
      }
    </script>
    <script>
      const setupReveal = () => {
        const elements = document.querySelectorAll("[data-reveal]");
        if (!elements.length) return;

        if (!("IntersectionObserver" in window)) {
          elements.forEach((el) => el.classList.add("reveal-visible"));
          return;
        }

        const observer = new IntersectionObserver(
          (entries) => {
            entries.forEach((entry) => {
              if (entry.isIntersecting) {
                entry.target.classList.add("reveal-visible");
              } else {
                entry.target.classList.remove("reveal-visible");
              }
            });
          },
          { threshold: 0.2 }
        );

        elements.forEach((el) => observer.observe(el));
      };

      if (document.readyState === "loading") {
        document.addEventListener("DOMContentLoaded", setupReveal);
      } else {
        setupReveal();
      }
    </script>
  </head>
  <body class="bg-primary dark:bg-dk-primary w-full container mx-auto">
    <Nav client:visible searchItems={searchItems} />
    <div class="container mx-auto px-4">
      <slot />
    </div>
    <FooterBar variant={footerVariant} />
    <ChatWidget client:only />
    <TopButton client:only />
    <Pointer client:only />
  </body>
</html>
